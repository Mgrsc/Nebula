# Server Configuration
PORT=3000
NODE_ENV=production

# Database
DATABASE_PATH=/app/data/nebula.db

# CORS Configuration (Production)
# Comma-separated list of allowed origins
# Example: ALLOWED_ORIGINS=https://example.com,https://app.example.com
# Leave empty or unset in development mode (NODE_ENV != production)
ALLOWED_ORIGINS=

# Web Distribution Path (Optional)
# Default: ../../web/dist relative to server/src
# WEB_DIST_PATH=/app/web/dist
